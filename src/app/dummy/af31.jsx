import React from "react";

const InlineStyle = () => {
  const style = {
    backgroundColor: "lightblue",
    padding: "20px",
    textAlign: "center",
  };

  return <div style={style}>This is styled with inline CSS!</div>;
};

export default InlineStyle;
